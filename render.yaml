services:
  - type: web
    name: authorized-surface-scanner
    runtime: python
    buildCommand: pip install .
    startCommand: python -m surface_scanner web --host 0.0.0.0 --port $PORT
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.11
